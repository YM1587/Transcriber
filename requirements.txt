# ── Core transcription ──────────────────────────────────────
faster-whisper>=1.0.3
# ── Audio I/O ───────────────────────────────────────────────
# soundfile reads the 16kHz mono WAV directly (no resample) —
# used by the optimized diarization path instead of librosa.load.
soundfile>=0.12.1
# ── Numeric / ML helpers ────────────────────────────────────
numpy>=1.24
librosa>=0.10.0          # MFCC features for offline diarization
scikit-learn>=1.3.0      # agglomerative clustering for diarization
# ── Web UI ──────────────────────────────────────────────────
gradio>=4.0.0
# Optional CLI diarization with pyannote (requires HF token):
#   pip install pyannote.audio torch
